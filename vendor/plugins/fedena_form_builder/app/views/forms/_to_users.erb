

<% string = @to_users.inject('') { |arr, u| arr + u.user_id.to_s + ',' }.chomp(',') %>
<div class="label-field-pair-form">
  <label for="student_grade"><%= t('select_recipient') %></label>
  <div class ="scrolable" >
    <div class="scroll-inside">
      <div class="hover">
        <%= link_to "#{t('select_all')}", '#', :onClick => "add_all_member('#{string}')", :class=> 'all' %>
        <%= link_to "#{t('none')}", '#', :onClick => "remove_all_member('#{string}')", :class=> 'all' %>
      </div>
      <% @to_users.each do |t| %>
        <% user_name = "<span>(#{t.user.username})&#x200E;</span><span>#{t('add')}</span>" %>
        <div class="hover"><%= link_to shorten_string(t.full_name,13)+user_name  , '#', :onClick => "add_member(#{t.user_id})", :class=>'individual',
            :before => "Element.show('loader')",
            :success => "Element.hide('loader')" %>
        </div>
      <% end %>
    </div>
  </div>
</div>



