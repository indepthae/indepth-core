en-ch:
  hostel_fee:
    no_batch_selected: "No %{cw_batch} selected"
    select_batch: "Select %{cw_batch}"
    select_a_batch: "Select a %{cw_batch}"
    batch_wise_fee_collections: "%{cw_batch_capital}-wise Fee Collections"
  hostels:
    pay_hostel_fee: "Pay hostel fee %{cw_batch}-wise"


  