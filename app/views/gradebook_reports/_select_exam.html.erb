<div class="label-field-pair">
      <label for="exam" class="label-exam"><%= t('exam_text') %></label>
      <span class="student_selector">
          <%= select :subject_report, :exam,grouped_options_for_select(@list),
        {:include_blank =>t('select_exam')},
        {:onChange => "#{remote_function(:url => {:action => "reload_report_type"},
         :with => "'exam_id='+ value"
        )}"} 
      %>
      </span>
</div>
