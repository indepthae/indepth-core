<div class="label-field-pair">
    <label for="exam" class="label-exam"><%= t('exam_text') %></label>
    <span class="student_selector">
        <%= select :consolidated_exam_report, :exam,grouped_options_for_select(@list),
          {:include_blank =>t('select_exam')},
          {:onChange => "#{remote_function(:url => {:action => "reload_cosolidated_exam_types"},
          :with => "'exam_id='+ value"
          )}"} 
      %>
    </span>
</div>
