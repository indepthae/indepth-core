
<div id="content-header">
    <%= show_header_icon %>
    <h1><%= t('view_refunds') %></h1>
    <div class='header-sep'>|</div>
    <div class='sub-header'><%= t('view_refunds') %></div>

</div>

<div id="page-yield">
    <div class="bread_crumb">
        <% if @student.present? %>
          <%breadcrumb :finance_refund_student_view,@student%>
        <% else %>
          <% breadcrumb :archived_student_profile,@archived_student  %>
        <% end %>
        <%=render_breadcrumbs%>
    </div>
    <%= render :partial => 'finance/refund_student_view' %>
</div>