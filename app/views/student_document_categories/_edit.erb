<% form_remote_for @category, :url => student_document_category_path(@category) do |form| %>
  <%= render :partial => "category_form", :locals => {:form => form} %>
  <%= form.submit  "", :value => "#{t('update_category')}", :class => "submit_button", :id => "submit_button" %>
<% end %>