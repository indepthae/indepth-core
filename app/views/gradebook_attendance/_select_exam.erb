<div class="label-field-pair">
    <div class="label">
        <label for="batch_id" class="label-batch"><%= t('exam_text') %></label>
    </div>
    <span class="batch_selector">
        <%= select :gradebook_attendance,:linkable_id, grouped_options_for_select(@list),
          {:include_blank=>t('select_exam')},
          {:onChange => "#{remote_function(:url => {:action => "load_button"},
          :with => "'sub_type='+value")}"}
      %>
    </span>
</div>
<%= hidden_field_tag 'gradebook_attendance[linkable_type]','exam' %>